from setuptools import setup, find_packages

setup(
    name="bots",  # Replace with your project name
    version="0.1",
    packages=find_packages(),  # Automatically find packages
    include_package_data=True,
    install_requires=[],  # Add any dependencies here
)